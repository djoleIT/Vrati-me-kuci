import CompareClient from "./CompareClient";

export const metadata = {
  title: "QR privezak ili čipovanje — šta je bolje za psa? | Vrati Me Kući",
  description:
    "Pošteno poređenje: čip zahteva veterinarski skener, QR kod čita svako sa telefonom, odmah. Najbolje je koristiti oba — evo zašto.",
};

export default function Page() {
  return <CompareClient />;
}
